const checkUsersValid = (goodUsers) => allUsersValid = (submittedUsers) => {
    // console.log(goodUsers[0])
    console.log(submittedUsers.id)
    // return submittedUsers.every(userId => userId === goodUsers)
}
module.exports = checkUsersValid
